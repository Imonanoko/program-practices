use std::collections::LinkedList;
use std::option::Option::Some;
pub(crate) fn main() {
    let mut i:i32=0;
    while i>5 {
        i+=1;
        println!("{}",i);

        loop{
            break;
        }
    }
}
